services:
  - type: static
    name: resuradar-frontend
    env: static
    buildCommand: npm install && npm run build-prod
    staticPublishPath: dist/resume-analyzer-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
